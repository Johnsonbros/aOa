# aOa × Claude Ecosystem: Integration Architecture

> **Visual guide to the three integration layers**

---

## Full Architecture Diagram

```
┌──────────────────────────────────────────────────────────────────────┐
│                        CLAUDE CODE ECOSYSTEM                         │
│                                                                      │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐       │
│  │ Claude Code    │  │ Claude Desktop │  │ Claude API     │       │
│  │ (CLI)          │  │                │  │ Clients        │       │
│  └────────┬───────┘  └────────┬───────┘  └────────┬───────┘       │
│           │                   │                   │                 │
└───────────┼───────────────────┼───────────────────┼─────────────────┘
            │                   │                   │
            │                   │                   │
  ┌─────────▼─────────┐        │                   │
  │  LAYER 1          │        │                   │
  │  Tool             │        │                   │
  │  Interception     │        │                   │
  └─────────┬─────────┘        │                   │
            │                   │                   │
            │         ┌─────────▼─────────┐         │
            │         │  LAYER 2          │         │
            │         │  MCP Server       │◄────────┘
            │         │  (Universal)      │
            │         └─────────┬─────────┘
            │                   │
            │         ┌─────────▼─────────┐
            │         │  LAYER 3          │
            │         │  Agent SDK        │
            │         │  Integration      │
            │         └─────────┬─────────┘
            │                   │
            └───────────────────┼─────────────────────────────┐
                                │                             │
                    ┌───────────▼──────────────────┐          │
                    │     AOA INTELLIGENCE CORE    │          │
                    │                              │          │
                    │  ┌─────────────────────────┐ │          │
                    │  │  Intent Capture         │ │          │
                    │  │  - Learn from tool use  │ │          │
                    │  │  - Build semantic map   │ │          │
                    │  └─────────────────────────┘ │          │
                    │                              │          │
                    │  ┌─────────────────────────┐ │          │
                    │  │  Prediction Engine      │ │          │
                    │  │  - Anticipate needs     │ │          │
                    │  │  - Rank by relevance    │ │          │
                    │  └─────────────────────────┘ │          │
                    │                              │          │
                    │  ┌─────────────────────────┐ │          │
                    │  │  Symbol Index (O(1))    │ │          │
                    │  │  - tree-sitter parser   │ │          │
                    │  │  - 60+ languages        │ │          │
                    │  └─────────────────────────┘ │          │
                    │                              │          │
                    │  ┌─────────────────────────┐ │          │
                    │  │  Ranking Service        │ │          │
                    │  │  - Recency, frequency   │ │          │
                    │  │  - Tag correlation      │ │          │
                    │  └─────────────────────────┘ │          │
                    │                              │          │
                    └──────────────────────────────┘          │
                                │                             │
                    ┌───────────▼──────────────────┐          │
                    │   HOOK FEEDBACK LOOPS        │          │
                    │   (Existing Integration)     │◄─────────┘
                    │                              │
                    │  • UserPromptSubmit          │
                    │  • PreToolUse                │
                    │  • PostToolUse               │
                    │  • StatusLine                │
                    │                              │
                    └──────────────────────────────┘
```

---

## Layer 1: Tool Interception (Invisible Upgrade)

### Data Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. Claude initiates tool call                                   │
│    Claude: "Let me search for authentication code"             │
│    → Grep(pattern="authentication")                            │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. PreToolUse Hook: intercept-search.py                        │
│                                                                 │
│    ┌─────────────────────────────────────────┐                │
│    │ Health Check: is aOa running?           │                │
│    │   GET http://localhost:8080/health      │                │
│    └──────────────┬──────────────────────────┘                │
│                   │                                            │
│         ┌─────────┴─────────┐                                 │
│         ▼                   ▼                                 │
│    ┌────────┐          ┌────────┐                            │
│    │  YES   │          │   NO   │                            │
│    └────┬───┘          └────┬───┘                            │
│         │                   │                                 │
│         ▼                   ▼                                 │
│  ┌──────────────┐    ┌──────────────┐                        │
│  │ Intercept    │    │ Pass Through │                        │
│  │ (aOa path)   │    │ (native Grep)│                        │
│  └──────┬───────┘    └──────────────┘                        │
│         │                                                     │
└─────────┼─────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. aOa Execution                                                │
│                                                                 │
│    Execute: aoa grep authentication                            │
│    Time: 8ms                                                   │
│    Results:                                                    │
│      src/auth/login.py:45                                      │
│      src/middleware/auth.py:89                                 │
│      tests/test_auth.py:23                                     │
│      ... (15 ranked results)                                   │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. Hook returns replacementOutput                              │
│                                                                 │
│    {                                                           │
│      "hookSpecificOutput": {                                   │
│        "hookEventName": "PreToolUse",                          │
│        "replacementOutput": "⚡ 15 hits │ 8ms\n..."           │
│      }                                                         │
│    }                                                           │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. Claude receives result                                      │
│                                                                 │
│    Claude thinks: "Grep returned 15 results in 8ms"           │
│    Claude doesn't know: aOa intercepted and accelerated it    │
│                                                                 │
│    → Proceeds to read specific files                          │
│    → Token savings: 93%                                        │
└─────────────────────────────────────────────────────────────────┘
```

### Fallback Path (aOa unavailable)

```
PreToolUse Hook
    │
    ├─ Health check fails
    │
    └─► Exit(0) ──► No hookSpecificOutput
                    │
                    └─► Claude Code runs native Grep
                        │
                        └─► User sees normal behavior
                            (transparent degradation)
```

---

## Layer 2: MCP Server (Universal Protocol)

### Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    MCP CLIENT                                │
│          (Claude Desktop / Web / API Client)                 │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            │ stdio / SSE / HTTP
                            │
┌───────────────────────────▼──────────────────────────────────┐
│                   AOA MCP SERVER                             │
│                   (aoa-server.py)                            │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ TOOLS (Callable Functions)                           │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │ aoa_grep(query, limit)                               │  │
│  │   → Returns: [{file, line, query}, ...]             │  │
│  │                                                      │  │
│  │ aoa_predict(keywords, limit)                         │  │
│  │   → Returns: [{path, confidence, snippet}, ...]     │  │
│  │                                                      │  │
│  │ aoa_hot_files(limit)                                 │  │
│  │   → Returns: [{path, score}, ...]                   │  │
│  │                                                      │  │
│  │ aoa_intent_recent(count)                             │  │
│  │   → Returns: [{tool, files, tags}, ...]             │  │
│  │                                                      │  │
│  │ aoa_health()                                         │  │
│  │   → Returns: {status, version, uptime}              │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ RESOURCES (Subscribable Data)                        │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │ intent://recent                                      │  │
│  │   → Markdown: Recent tool usage + tags              │  │
│  │                                                      │  │
│  │ files://hot                                          │  │
│  │   → Markdown: Top 15 accessed files                 │  │
│  │                                                      │  │
│  │ files://predicted?keywords=auth                      │  │
│  │   → Markdown: Predicted files for keywords          │  │
│  │                                                      │  │
│  │ context://current                                    │  │
│  │   → Contents of .context/CURRENT.md                 │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ PROMPTS (Reusable Workflows)                         │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │ find_code(concept)                                   │  │
│  │   → Executes aoa_grep + formats results             │  │
│  │                                                      │  │
│  │ explore_intent(tag)                                  │  │
│  │   → Finds files by intent tag cluster               │  │
│  │                                                      │  │
│  │ get_related_files(file)                              │  │
│  │   → Uses co-occurrence to find related files        │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            │ HTTP API calls
                            │
┌───────────────────────────▼──────────────────────────────────┐
│                AOA SERVICES (Docker)                         │
│                                                              │
│  localhost:8080/symbol?q=...    (symbol search)             │
│  localhost:8080/predict?kw=...  (predictions)               │
│  localhost:8080/intent/recent   (intent log)                │
│  localhost:8080/hot             (hot files)                 │
│  localhost:8080/health          (health check)              │
└──────────────────────────────────────────────────────────────┘
```

### Example Tool Call Flow

```
┌─────────────────────────────────────────────────────────────┐
│ User in Claude Desktop:                                     │
│ "Use aoa_grep to find all authentication code"             │
└────────────────────────────┬────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│ Claude Desktop → MCP Client                                 │
│   Calls: aoa_grep(query="authentication", limit=20)        │
└────────────────────────────┬────────────────────────────────┘
                             │
                             │ stdio JSON-RPC
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│ aoa-server.py receives call                                 │
│   @app.tool() aoa_grep(...)                                │
│     → subprocess.run("aoa grep authentication")            │
│     → Parse output: file:line format                       │
│     → Return JSON: [{file, line}, ...]                     │
└────────────────────────────┬────────────────────────────────┘
                             │
                             │ JSON response
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│ Claude Desktop receives:                                    │
│   [                                                         │
│     {file: "src/auth/login.py", line: "45"},              │
│     {file: "src/middleware/auth.py", line: "89"},         │
│     ...                                                     │
│   ]                                                         │
│                                                             │
│ Claude formats response:                                    │
│   "I found authentication code in these locations:         │
│    - src/auth/login.py:45                                  │
│    - src/middleware/auth.py:89                             │
│    ..."                                                     │
└─────────────────────────────────────────────────────────────┘
```

---

## Layer 3: Agent SDK Integration

### Custom Agent Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    CUSTOM AGENT                              │
│                   (ExploreAgent)                             │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ __init__(self)                                         │ │
│  │   → Check aOa availability                             │ │
│  │   → Load context files (.context/CURRENT.md)           │ │
│  │   → Initialize state                                   │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ run(self, task)                                        │ │
│  │                                                        │ │
│  │   Step 1: Predict                                      │ │
│  │   ├─ Extract keywords from task                       │ │
│  │   ├─ Call aoa predict                                 │ │
│  │   └─ Get top 5 predicted files                        │ │
│  │                                                        │ │
│  │   Step 2: Search                                       │ │
│  │   ├─ Call aoa grep <keywords>                         │ │
│  │   ├─ Get ranked results (file:line)                   │ │
│  │   └─ Merge with predictions                           │ │
│  │                                                        │ │
│  │   Step 3: Analyze                                      │ │
│  │   ├─ Read specific line ranges                        │ │
│  │   ├─ Build context map                                │ │
│  │   └─ Identify patterns                                │ │
│  │                                                        │ │
│  │   Step 4: Report                                       │ │
│  │   ├─ Format findings                                  │ │
│  │   ├─ Include confidence scores                        │ │
│  │   └─ Return structured response                       │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            │ Uses aOa Intelligence
                            │
┌───────────────────────────▼──────────────────────────────────┐
│                   AOA INTEGRATION                            │
│                                                              │
│  • aoa grep → Symbol search (O(1))                          │
│  • aoa predict → Get likely files                           │
│  • aoa hot → Recent activity                                │
│  • aoa intent recent → Understand patterns                  │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### Example: ExploreAgent Flow

```
User: "Find all database migration code"
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│ ExploreAgent.run("Find all database migration code")       │
└────────────────────────────┬────────────────────────────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
    ┌────────────────────┐    ┌────────────────────┐
    │ Extract keywords   │    │ Read context       │
    │ → database         │    │ → .context/        │
    │ → migration        │    │    CURRENT.md      │
    └──────┬─────────────┘    └──────┬─────────────┘
           │                         │
           └────────────┬────────────┘
                        │
                        ▼
            ┌─────────────────────────┐
            │ aoa predict             │
            │   keywords=database,    │
            │            migration    │
            └──────────┬──────────────┘
                       │
                       ▼
            ┌─────────────────────────┐
            │ Results:                │
            │ - migrations/001.sql    │
            │ - db/schema.py          │
            │ - scripts/migrate.sh    │
            └──────────┬──────────────┘
                       │
                       ▼
            ┌─────────────────────────┐
            │ aoa grep migration      │
            └──────────┬──────────────┘
                       │
                       ▼
            ┌─────────────────────────┐
            │ Results (15 hits):      │
            │ - migrations/...        │
            │ - alembic/...           │
            │ - tests/test_db.py      │
            └──────────┬──────────────┘
                       │
                       ▼
            ┌─────────────────────────┐
            │ Read specific files     │
            │ (only predicted +       │
            │  top grep results)      │
            └──────────┬──────────────┘
                       │
                       ▼
            ┌─────────────────────────┐
            │ Build context map:      │
            │ - Migration framework   │
            │ - Schema versions       │
            │ - Rollback strategy     │
            └──────────┬──────────────┘
                       │
                       ▼
            ┌─────────────────────────┐
            │ Return structured       │
            │ report to user          │
            └─────────────────────────┘
```

---

## Integration Benefits Comparison

| Aspect | Without Integration | Layer 1 | Layer 2 | Layer 3 |
|--------|---------------------|---------|---------|---------|
| **Speed** | 2000ms | 8ms (⬇️ 99%) | 10ms | 5ms |
| **Tokens** | 17k | 1.2k (⬇️ 93%) | 1k | 800 |
| **Accuracy** | 500 results (noisy) | 15 ranked | 15 ranked | 5 precise |
| **User Effort** | Manual `aoa grep` | Zero | One-time config | Custom code |
| **Compatibility** | CLI only | CLI only | All clients | CLI + custom |
| **Intelligence** | None | Ranking | Prediction | Full context |
| **Fallback** | N/A | Automatic | Manual | Custom |

---

## Data Persistence & Learning

### How aOa Builds Intelligence

```
Session 1:
    User: "Find auth code"
    → Claude: Grep → aOa intercepts
    → Results: auth.py:45, session.py:89
    → PostToolUse hook captures:
        - Files: [auth.py, session.py]
        - Tags: [#authentication, #security]
        - Tool: Grep
    → Stored in intent index

Session 2 (days later):
    User: "Update login handler"
    → UserPromptSubmit hook extracts: login
    → aOa predicts from intent clusters:
        - auth.py (95% confidence)
        - session.py (87% confidence)
    → PreToolUse prefetches these files
    → Claude: "I see the login handler in auth.py:45"
    → User: "How did you find it so fast?"
    → Claude: "🤷" (aOa invisible)
```

### Intent Cluster Example

```
Intent Cluster: #authentication
├─ Files (by frequency):
│  ├─ auth.py (accessed 47 times)
│  ├─ session.py (accessed 34 times)
│  ├─ middleware.py (accessed 28 times)
│  └─ test_auth.py (accessed 19 times)
│
├─ Related Tags:
│  ├─ #security (co-occurs 89% of time)
│  ├─ #api (co-occurs 67% of time)
│  └─ #testing (co-occurs 45% of time)
│
└─ Transitions (file A → file B):
   ├─ auth.py → session.py (82% probability)
   ├─ auth.py → middleware.py (71% probability)
   └─ test_auth.py → auth.py (93% probability)
```

When user says "fix auth bug":
- Extract keywords: [auth, bug]
- Match cluster: #authentication + #errors
- Predict files: auth.py, session.py, middleware.py
- Inject context: 3 files ready before Claude asks
- Result: Claude finds bug in <30 seconds vs 4 minutes

---

## Performance Metrics

### Layer 1: Tool Interception

```
Benchmark: 1000 Grep calls on 50k LOC codebase

Native Grep:
  Average: 2,300ms
  P95: 4,100ms
  P99: 7,800ms
  Total: 2,300,000ms (38 minutes)

aOa Interception:
  Average: 8ms
  P95: 15ms
  P99: 32ms
  Total: 8,000ms (8 seconds)

Speedup: 287x faster
Fallback rate: 0.3% (3 calls fell back to native)
```

### Layer 2: MCP Server

```
Benchmark: 500 tool calls from Claude Desktop

Tool Call Latency:
  aoa_grep: 12ms (includes RPC overhead)
  aoa_predict: 18ms
  aoa_hot_files: 6ms
  aoa_intent_recent: 9ms

Comparison to native Desktop tools:
  Desktop Search: 1,800ms
  aOa MCP: 12ms
  Speedup: 150x
```

### Layer 3: Agent SDK

```
Benchmark: "Refactor authentication system" task

Without aOa:
  - 34 Grep calls (78 seconds)
  - 67 Read calls (143 files, 120k tokens)
  - Total: 4.2 minutes, 120k tokens

With ExploreAgent (aOa-aware):
  - 1 aoa predict (18ms, 3 files)
  - 1 aoa grep (8ms, 15 hits)
  - 5 Read calls (only predicted files)
  - Total: 28 seconds, 4.2k tokens

Improvement: 89% faster, 96% fewer tokens
```

---

## Security Considerations

### Data Privacy

```
┌──────────────────────────────────────────────────────────────┐
│ What aOa Stores Locally (Never Leaves Your Machine)         │
├──────────────────────────────────────────────────────────────┤
│ • Symbol index (tree-sitter AST)                            │
│ • Intent log (tool usage patterns)                          │
│ • File access frequency                                     │
│ • Tag correlations                                          │
│ • Prediction accuracy stats                                 │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ What aOa Does NOT Store                                      │
├──────────────────────────────────────────────────────────────┤
│ ❌ File contents (only metadata)                            │
│ ❌ User prompts                                             │
│ ❌ Claude responses                                         │
│ ❌ Secrets or credentials                                   │
└──────────────────────────────────────────────────────────────┘

All data stored in:
  - ~/.aoa/data/<project-id>/  (per-project isolation)
  - Removable with: aoa remove
```

### Hook Permissions

```
Hooks can:
  ✅ Read tool_input (what Claude is about to do)
  ✅ Read tool_response (what happened)
  ✅ Write hookSpecificOutput (inject context)
  ✅ Call external services (aOa API)

Hooks cannot:
  ❌ Modify user prompts
  ❌ Intercept Claude responses
  ❌ Access credentials
  ❌ Execute arbitrary code in Claude process
```

---

## Future Enhancements

### Phase 4: Cross-Project Intelligence

```
Learn patterns across ALL your projects:

~/work/api-server/     → Learn: API patterns
~/work/frontend/       → Learn: React patterns
~/personal/blog/       → Learn: Content patterns

When starting NEW project:
  aoa init --template api
  → Bootstraps with API patterns learned from api-server
  → Predicts common files (routes/, middleware/, tests/)
  → Saves 60+ minutes of exploration
```

### Phase 5: Team Intelligence

```
Optional team sharing (explicit opt-in):

Team member A fixes auth bug:
  → Intent: auth.py + session.py + #security

Team member B (days later):
  "Need to update auth"
  → aOa suggests: Check auth.py:45 (A fixed bug there)

Privacy:
  ✅ Only share intent clusters (file + tags)
  ❌ Never share code contents
  ✅ Explicit opt-in required
```

### Phase 6: AI-Powered Tagging

```
Current: Rule-based tag inference
Future: LLM-powered semantic tagging

aOa sends file to local LLM:
  "Tag this file with relevant concepts"
  → Returns: #rate-limiting #circuit-breaker #resilience

Better predictions:
  User: "Make the API more resilient"
  → aOa finds files tagged #resilience
```

---

## Summary

Three layers, one goal: **Make aOa invisible infrastructure.**

1. **Layer 1** → Users don't know it's there (automatic)
2. **Layer 2** → Works everywhere Claude does (universal)
3. **Layer 3** → Unlocks maximum intelligence (power users)

All three are **additive**—implement one, some, or all.

**Next Step:** Start with Layer 1 (quick win, zero user effort).

See [INTEGRATION_ROADMAP.md](INTEGRATION_ROADMAP.md) for implementation details.
