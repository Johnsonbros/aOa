# aOa Environment Configuration
# Copy this file to .env and adjust values for your environment

# Root directory for projects (default: user's home directory)
PROJECTS_ROOT=${HOME}

# Port for the aOa gateway service (default: 8080)
GATEWAY_PORT=8080

# Path to the codebase being analyzed (default: current directory)
CODEBASE_PATH=.
